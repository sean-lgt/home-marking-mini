<!-- components/detail-chat/index.wxml -->
<view class="container">
  <i-avatar src="{{userInfo.avatar}}" shape="circle" size="60" text="{{userInfo.nickname}}"></i-avatar>
  <view bind:tap="handleToChat">
    <i-button special="{{true}}" bgColor="#fff" icon="telephone">联系对方</i-button>
  </view>
</view>